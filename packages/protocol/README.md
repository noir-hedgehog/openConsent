# openConsent runtime protocol (alpha)

The runtime adapters share one vocabulary: policy, preference snapshot, immutable receipt, decision, and propagation event. See `conformance.json` for cross-language fixtures.

Browser state is demonstrative and is not authoritative evidence. Production implementations must use a protected receipt store, server-observed GPC, version checks, access controls, retention rules, and a reviewed signing strategy.
